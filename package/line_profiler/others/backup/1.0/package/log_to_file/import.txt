import sys
from os.path import join, abspath, dirname

if join(abspath(dirname(__file__)), "package", "log_to_file") not in sys.path: 
    sys.path.insert(0, join(abspath(dirname(__file__)), "package", "log_to_file"))

from log_to_file import log_to_file, print